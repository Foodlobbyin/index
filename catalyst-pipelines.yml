version: 1
jobs:
  build-and-deploy:
    steps:
      - cd backend
      - npm install
      - npm run build
      - cd ..
      - npm install -g zcatalyst-cli@beta
      - catalyst deploy appsail --name foodlobby-backend --build-path backend/dist --stack node20 --token $CATALYST_AUTH_TOKEN
stages:
  - name: deploy
    jobs:
      - build-and-deploy