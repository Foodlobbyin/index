version: 1
jobs:
  dump-env:
    steps:
      - env | grep -i catalyst || true
      - env | grep -i token || true
      - env | grep -i zoho || true
      - env | grep -i auth || true
stages:
  - name: diagnose
    jobs:
      - dump-env