version: 1
jobs:
  diagnose:
    steps:
      - npm install -g zcatalyst-cli
      - catalyst -v
      - catalyst help deploy || true
      - catalyst deploy --help || true
stages:
  - name: debug
    jobs:
      - diagnose