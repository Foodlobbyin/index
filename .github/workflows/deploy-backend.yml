# Backend is deployed to Cloudflare Workers via deploy-worker.yml
# This Catalyst AppSail workflow is disabled — backend has migrated to Cloudflare Workers.
# Kept as documentation only.

name: Deploy Backend to Catalyst AppSail (DISABLED)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'This workflow is disabled. Backend runs on Cloudflare Workers.'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is disabled. Backend is deployed via Cloudflare Workers (deploy-worker.yml)."
